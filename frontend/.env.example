# Local development (default if unset). Prefer 127.0.0.1 over localhost (IPv6).
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000/api/v1

# Production (set in Vercel Project Settings → Environment Variables, then redeploy):
# NEXT_PUBLIC_API_URL=https://YOUR-BACKEND.onrender.com/api/v1
